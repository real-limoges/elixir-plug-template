defmodule Relay do
  @moduledoc """
  Relay is a backend service: a JSON API (Plug + Bandit) that turns requests into
  asynchronous, queued jobs through a strict layered pipeline.

  See `CLAUDE.md` for the architecture and conventions.
  """
end
